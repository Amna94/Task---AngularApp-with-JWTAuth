import { PermissionDetail } from './permission-detail.model';

describe('PermissionDetail', () => {
  it('should create an instance', () => {
    expect(new PermissionDetail()).toBeTruthy();
  });
});
